import { handleApiCall } from './callApi';
import { handleErrorApi } from './handleErrorApi';
import {
    Image,
    Root,
    Filter,
    Paging,
} from './interface';

export {
    handleApiCall,
    handleErrorApi,
};

export type {
    Image,
    Root,
    Filter,
    Paging,
};
