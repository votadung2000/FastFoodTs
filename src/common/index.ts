import { handleApiCall } from './callApi';
import { handleErrorApi } from './handleErrorApi';
import {
    Image,
    Root,
    Paging,
} from './interface';

export {
    handleApiCall,
    handleErrorApi,
};

export type {
    Image,
    Root,
    Paging,
};
