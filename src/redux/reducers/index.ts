import {
    authReducer,
    authSelector,
    addAuth,
} from './auth';

import {
    animatedMenuReducer,
    showMenu,
    animatedMenuSelector,
} from './animated_menu';

export {
    // auth
    authReducer,
    authSelector,
    addAuth,

    // animated_menu
    animatedMenuReducer,
    showMenu,
    animatedMenuSelector,
};
